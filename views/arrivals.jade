extends layout

block content
	h1= title
	p= 'Last flight viewed ' + lastNumber
	ul
		each flight,index in arrivals
			- landed = new Date(flight.arrivedAt)
			li= flight.number + ": " + flight.origin + "-" + landed